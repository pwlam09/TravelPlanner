<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state. -->
    <item android:drawable="@color/activity_pressed_bg"
        android:state_pressed="true" />
    <!-- Selected state. -->
    <item android:drawable="@color/activity_selected_bg"
        android:state_activated="true" />
    <!-- Normal state. --><item android:drawable="@color/activity_default_bg" />
</selector>